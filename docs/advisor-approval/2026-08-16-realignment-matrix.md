# Automated Phishing Detection for Frontier AI Inference

**Status:** The August 20 advisor report directed continuation of the three-question design with objective, reproducible outcome criteria. PhiUSIIL development-data preparation and its aggregate record are complete. The current technical milestone is frozen raw-URL feature extraction and `length-only` and `Logistic-L1` baseline implementation using only the train and validation partitions. H1, H2, and H3 remain undecided; no confirmatory experiment has been run. No PhishVN record has been accessed.

**Version:** 1.3 | **Date:** 2026-09-03

## Study Plan

### Threat model

The unit of analysis is one raw URL carried through model input, retrieval or tool context, or model output at a frontier-AI inference gateway. An adversary may choose the URL string and vary its domain and character composition, while phishing campaigns can also change over time. The detector observes only the raw URL and features derived from it. It does not fetch the URL, inspect the destination page, use DNS or WHOIS lookups, or inspect private request content.

The security objective is to identify phishing URLs at a low false-positive operating point while preserving gateway performance. For schema-valid requests, the gateway returns an `allow` or `alert` decision. It sends uncertain requests, and requests observed during a detected shift, to the second-stage model; it does not perform automatic blocking, takedown, or other remediation. Schema-invalid requests follow the frozen FastAPI validation behavior and are not relabeled as phishing.

### Problem and thesis

Random row splits can overstate URL-classification performance when related domains occur in both training and test data, and a single fixed model may not remain reliable under temporal or domain shift. Running a transformer for every request may improve representation capacity but impose unnecessary operational cost.

I will test three linked claims. First, whether structural URL features and a compact character-level transformer improve low-FPR recall across domain-disjoint and external source/domain evaluation. Second, whether unsupervised shift monitoring can identify when broader transformer use is warranted. Third, whether selective execution can remain within two percentage points of transformer-only recall while meeting the stated invocation, latency, and error targets. These claims are prospective and will be evaluated under the decision rules below.

### Contribution boundary

The [research basis](../research-basis.md) documents the established components and closest current prior work: structural and character-based URL models, domain-disjoint and external evaluation, calibrated selective cascades, and security drift monitoring. The defensible contribution is the narrow prospective joint systems evaluation of a frozen structural-to-character-transformer URL cascade whose future routing changes after a validation-calibrated GMM alert under external source/domain shift, evaluated jointly against FPR, transformer-invocation, real-HTTP p95, and request-error gates. No priority is claimed for any individual component or method.

The GMM observes a change in the input distribution `P(X)` consistent with covariate/source shift. Its alert does not by itself prove harmful drift, label shift, concept drift, or causal performance degradation. Comparison of downstream detection errors tests whether the alert is useful for routing.

## Method and Evidence

### Outcome-label contract

All defined binary outcome labels are normalized to the local field `is_phishing`; the Tranco control designation remains separate as specified below. A local value of `is_phishing=1` means that the publisher or source designates the record as phishing; `is_phishing=0` means that the publisher or source designates it as legitimate. As detailed in the [research basis](../research-basis.md), these are publisher-provided, source-derived reference classifications, not independently verified ground truth. Neither Krti nor another individual assigns, overrides, or changes a confirmatory outcome.

### Mechanical mapping and quarantine rules

- PhiUSIIL is the only dataset used for model fitting and validation. Its registrable-domain group-test partition is the held-out internal evaluation and H3 replay source. Native label `0` maps to local `is_phishing=1`; native label `1` maps to local `is_phishing=0`. Only the raw URL and features derived locally from that URL are used.
- PhishVN NCSC `gold` records designated phishing map to local `is_phishing=1` and certified trusted-registry records designated legitimate map to local `is_phishing=0`; these are the primary external strata.
- PhishVN NCSC `silver` and ChongLuaDao/OpenPhish `bronze` records designated phishing map to local `is_phishing=1` only for secondary or sensitivity analyses.
- PhishVN Tranco rows retain a separate reference-negative-control designation. They are not treated as verified benign and do not supply primary inferential evidence; their control alert rate is only the mandatory secondary safeguard specified in H3.
- Inferred scenario labels and rule-derived abuse fields never determine or alter the binary outcome.
- Common rules: quarantine an invalid or missing URL; a canonical URL with conflicting normalized mappings, including conflicting source classifications where source-class provenance exists; a registrable-domain group assigned to more than one development split or occurring in more than one published external split; or an external registrable domain that overlaps PhiUSIIL. Quarantine the entire affected canonical-URL or registrable-domain group rather than choosing a label. For identical duplicates with the same mapping, retain the record with the lexicographically smallest stable identifier, whether a published record ID or deterministic row identifier; if no stable identifier exists, quarantine the entire duplicate group. Record the reason and count for every quarantine category. Neither Krti nor another individual adjudicates an outcome or exception.
- PhiUSIIL-specific rules: require a valid URL, a native label exactly equal to `0` or `1`, a split assignment, and either a stable record ID or a deterministic row identifier. Quarantine a record or affected group when any applicable input or predicate fails. PhiUSIIL does not require source, source-class, confidence-tier, or published-source fields.
- PhishVN-specific rules: require a valid URL, source, source class, confidence tier, published split, and stable published record ID. The source-class/tier combination must match the defined mapping above. Quarantine a record or affected group when a required value is absent or unknown or when the combination is not defined.

No numeric encoding is assumed for PhishVN. At the external-evaluation freeze, its schema, provenance fields, and label encoding are verified before any record is processed. If the verified schema cannot support the defined mapping without interpretation, the external evaluation stops and the discrepancy is recorded. Mapping and exclusion counts are frozen before model predictions or inferential results are examined.

### H3 request-error construct

- The denominator is all 50,000 measured concurrency-64 requests across five runs of 10,000 requests each. The 1,000 warm-up requests in each run are excluded. No timeout or connection failure is dropped from the measured denominator.
- The numerator is every measured request that fails to return HTTP 200 with syntactically valid JSON and all required `/v1/scan` response fields valid within a frozen 2,000 ms client timeout. It includes connection refusal or reset, client timeout, any non-2xx response, malformed JSON, and a missing or invalid required response field.
- A valid `alert` or `allow` response is not a request error. An incorrect phishing decision is a detection error, not a request error.
- The 2,000 ms client timeout and this exact request-error construct are prospective design choices frozen in protocol v1.3 before operational replay.

| Element | Specification |
|---|---|
| Development data | PhiUSIIL is the only dataset used for model fitting and validation. A conservative canonical key is used only for duplicate handling: absolute HTTP/HTTPS; lowercase scheme and IDNA-normalized host; normalized numeric port with default ports removed; empty path represented as `/`; percent-escape hex uppercased without decoding; and user information, path spelling, query order, and fragment preserved. Invalid URLs and conflicting canonical groups follow the mechanical quarantine rules above. Retained registrable domains are ranked by `SHA256("phiusiil-domain-split-v1\0" + "20260816\0" + domain)`. Hamilton largest-remainder apportionment converts the 70/15/15 target shares into integer domain-group counts for train, validation, and group-test, with ties ordered train, validation, group-test. The allocation is label-blind: it is not stratified, rerolled, or rebalanced. A registrable domain appears in one partition only. The group-test partition is the held-out internal evaluation and H3 replay source; it is not used for fitting, model selection, threshold selection, or monitor calibration. Only raw URLs and locally derived features enter the study. |
| External confirmation | This material amendment replaces proposed PhishVN v1, DOI `10.17632/b97hxbxtpd.1`, with current PhishVN v4, DOI `10.17632/b97hxbxtpd.4`. Relative to v1, v4 incorporates corrected data (the corrections landed in v3) and adds completed audit documentation; the v4 data are byte-identical to v3. PhishVN remains sealed until the protocol, data pipeline, models, thresholds, and analysis code are frozen. The external evaluation and monitoring population is the full published v4 test split in its published file order, after only quarantine required by the frozen common and PhishVN-specific rules above; this is the `frozen full-test stream` below. Every retained test row participates in label-blind routing. Source and confidence filters are applied only after routing to calculate outcome metrics. The population, ordering, routing-denominator, and outcome-denominator rules are frozen before access; realized mapping, exclusion, and denominator counts are frozen after quarantine and before predictions. NCSC `gold` positives and certified trusted-registry negatives are the primary external detection-error strata. NCSC `silver`, ChongLuaDao/OpenPhish `bronze`, Tranco reference-negative controls, inferred scenario labels, and rule-derived abuse fields have only the secondary or sensitivity roles defined above. Tranco is a popularity proxy/control, not verified benign, and its separately reported control alert rate is a mandatory secondary safeguard that can gate H3 without becoming primary evidence. Frozen models are scored without refitting or recalibration, and no numeric label encoding is assumed before schema verification at the external-evaluation freeze. |
| Detection models | Stage 1 is `Logistic-L1` on length/structure, character composition/entropy, authority syntax, and path/query features. Stage 2 is a compact character transformer over an IDNA-normalized ASCII URL and a fixed, training-derived character vocabulary. Input is capped at 256 characters by retaining the first 192 and last 64. The encoder has 4 layers, width 192, 6 attention heads, feed-forward width 768, and dropout 0.1. Training uses weighted binary cross-entropy, AdamW, and early stopping on validation average precision. Seed `42` is primary; seeds `42`, `43`, `44`, `45`, and `46` form the sensitivity analysis. Random Forest remains a secondary benchmark and does not determine a hypothesis decision. |
| Calibration | Preprocessing is fitted on training data, and model selection is completed without test access. On validation data only, each model threshold maximizes recall subject to the exact one-sided 95% Clopper-Pearson FPR upper bound being <= 1%; ties use the smaller FPR upper bound and then the higher threshold. For the cascade, candidate symmetric bands contain all validation scores within each unique absolute distance from the locked `Logistic-L1` threshold. Select the band with the fewest escalations whose cascade recall is at least `recall(transformer) - 0.02` and whose FPR bound is <= 1%; if none qualifies, record that the target was not met rather than changing the rule. Test outcomes cannot change these choices. |
| Shift monitoring | The primary monitor is a diagonal-covariance GMM fitted to standardized structural features plus the stage-1 score from PhiUSIIL training data. Component count is selected by BIC over 1-6. PhiUSIIL validation domains are divided by a frozen domain-hash rule into separate monitor-calibration and false-alert-audit streams. Windows contain 256 requests with stride 64 and are scored by mean negative log-likelihood. The alert boundary is the 95th percentile of calibration-window scores; false alerts are measured only on the independent audit stream. MMD and PSI are secondary comparators. A GMM alert flags a distributional departure consistent with covariate/source shift; downstream error comparisons test its routing value. |
| Shift streams | The primary shift is explicitly an external source/domain shift: the PhishVN v4 `frozen full-test stream` relative to the PhiUSIIL reference. This is not described as pure within-source temporal drift. Controlled string-only URL perturbations are sensitivity evidence for obfuscation shift and cannot satisfy H2. |
| Window manifest | The PhishVN v4 `frozen full-test stream` contains the full published test split in published file order after only quarantine required by the frozen common and PhishVN-specific rules above. Every retained row participates in label-blind routing; source and confidence filters are applied only afterward when outcome metrics are calculated. The first 256 requests use the fixed cascade. Windows end at request 256 and every 64 requests thereafter. An alert routes exactly the next 256 future arrivals through the transformer; it never reroutes the records that produced the alert. Overlapping activations are unioned, a terminal activation is truncated at the end of the stream, and the transformer is invoked at most once per request. This is a prospective, future-only policy replay, not causal inference. Population, order, and denominator rules are frozen before access, and realized counts are frozen before model predictions or inferential results are examined. |
| Statistical analysis | Primary recall differences use a registrable-domain-clustered, 2,000-replicate percentile bootstrap with a 95% confidence interval. Resampling and paired predictions use seed `20260816`. Exact McNemar tests are secondary. Ablation-family p-values use Holm correction; neither analysis replaces a primary decision rule. |
| Validity controls | Before fitting, canonicalize PhiUSIIL and apply the common plus PhiUSIIL-specific mapping and quarantine rules above; do not require PhishVN-only provenance or tier fields. At the external-evaluation freeze, apply the common plus PhishVN-specific rules to the full published PhishVN v4 test split before model predictions or inferential results are examined. Only records quarantined under the applicable rules are removed from the `frozen full-test stream`; source or confidence designations never change routing membership. Mapping, quarantine-reason, exclusion, and denominator counts are frozen first. Source-only, formatting-only, and label-permutation checks test for shortcut learning. Missing or unknown applicable inputs, schema fields, encodings, or manifests stop the affected processing; no synthetic or imputed replacement may enter confirmatory fitting. |
| Secondary reporting | Report average precision, recall at 1% FPR, precision, F2, MCC, balanced accuracy, ROC-AUC, Brier score, calibration error, and expected alerts and misses per 10,000 requests at assumed prevalence of 0.1%, 1%, and 5%. These measures describe the result but do not replace the locked hypothesis rules. |
| Operational replay | The primary benchmark manifest contains 10,000 URLs from the held-out PhiUSIIL group-test partition at a declared 1% phishing prevalence, sampled by class without replacement with seed `20260816`; 0.1% and 5% prevalence manifests are sensitivity analyses, not estimates of gateway prevalence. This group-test partition is the H3 replay source and is not used for fitting or validation. H3 evaluates the fixed uncertainty cascade with drift monitoring held normal; drift-period routing and transformer-only worst-case load are reported separately. Real HTTP runs use concurrency 1, 8, 16, 32, 64, and 128, with 1,000 warm-ups and 10,000 measured requests in each of 5 runs. H3 evaluates latency and request errors only at concurrency 64. Its request-error denominator is exactly the 50,000 measured concurrency-64 requests; warm-ups are excluded, and every timeout or connection failure remains in the denominator. The numerator and frozen 2,000 ms client timeout follow the H3 request-error construct above. The detailed replay protocol is committed and hashed before any test processing. |

## Decision Matrix

| Item | Proposed question and hypothesis | Method | Primary evidence | Decision rule |
|---|---|---|---|---|
| **RQ1 / H1** | **RQ1:** What incremental value do structural URL features and character-level representations provide under registrable-domain-disjoint and external evaluation? **H1:** At the validation-selected FPR ceiling, full structural features improve recall over a length-only baseline, and the character-transformer cascade improves recall over `Logistic-L1`. | Fit length-only, `Logistic-L1`, transformer-only, and cascade models on PhiUSIIL training data. Use validation data only for thresholds, early stopping, and the cascade band, then score the untouched PhiUSIIL group test. After the external-evaluation freeze and schema verification, score every retained row in the PhishVN v4 `frozen full-test stream` once without detection-model adaptation; apply source and confidence filters only afterward to calculate outcome metrics. | Paired predictions on the PhiUSIIL group test; NCSC `gold` positives for primary external recall and certified trusted-registry negatives for primary external FPR. | Support H1 only if each compared model has observed FPR <= 1% on PhiUSIIL group-test negatives and certified trusted-registry negatives, and the domain-clustered 95% bootstrap lower bounds are greater than zero for both recall differences on PhiUSIIL group-test positives and NCSC `gold` positives: `recall(Logistic-L1) - recall(length-only)` and `recall(cascade) - recall(Logistic-L1)`. |
| **RQ2 / H2** | **RQ2:** Can GMM-based monitoring detect an external source/domain shift and guide escalation without exceeding the low-FPR operating constraint? **H2:** GMM detects at least 80% of prespecified external shift windows at no more than 5% false alerts, and prospective routing reduces the false-negative rate relative to the fixed cascade while retaining FPR <= 1%. | Fit the GMM on PhiUSIIL training features, set its boundary on one validation stream, and measure false alerts on a separate validation audit stream. Apply it without labels to every retained row in the PhishVN v4 `frozen full-test stream`, in published file order. Apply source and confidence filters only after routing for outcome metrics. At each alert, route only the next 256 future requests through the transformer. MMD, PSI, and controlled perturbations are secondary. This future-only policy replay is prospective and does not support a causal claim. | Window alerts plus paired fixed-cascade and prospective-policy predictions. Primary error inference uses NCSC `gold` positives and certified trusted-registry negatives. | Support H2 only if external-window detection is >= 80%, independent reference false alerts are <= 5%, alert-policy observed FPR on certified trusted-registry negatives is <= 1%, and the lower bound of a domain-clustered 95% bootstrap interval for the false-negative-rate reduction on NCSC `gold` positives is greater than zero. |
| **RQ3 / H3** | **RQ3:** What detection, escalation, throughput, and latency tradeoffs determine whether the fixed cascade is viable inline? **H3:** For each system, observed FPR on certified trusted-registry negatives is <= 1%, and the Tranco reference-negative alert rate is <= 1% as a mandatory secondary safeguard. Cascade recall on NCSC `gold` positives is noninferior to transformer-only recall within 2 percentage points. On the 1%-prevalence benchmark reference, transformer invocation is at most 30%, real-HTTP p95 latency is at most 200 ms at concurrency 64, and the request-error rate under the stated construct is below 0.1%. | Score the fixed cascade and transformer-only model at frozen thresholds on every retained row in the PhishVN v4 `frozen full-test stream`, in published file order. Apply source and confidence filters only afterward to calculate outcome metrics. Separately report the Tranco reference-negative alert rate as a mandatory secondary external safeguard that can gate H3 but is not primary evidence. Replay the fixed cascade over real HTTP at the declared benchmark prevalence and evaluate future-only shift-period routing separately under H2. Measure request errors with the stated 50,000-request denominator, failure numerator, and frozen 2,000 ms client timeout; do not drop timeouts or connection failures. | Paired recall on NCSC `gold` positives; primary FPR on certified trusted-registry negatives; the deterministic reference invocation trace; and five measured HTTP runs. | Support H3 only if, for each of the fixed cascade and transformer-only model, observed FPR on certified trusted-registry negatives is <= 1% and the Tranco reference-negative alert rate is <= 1% as a mandatory secondary safeguard. Report exact one-sided 95% Clopper-Pearson upper confidence bounds for the two certified-registry FPR estimates and the two Tranco control alert-rate estimates. The domain-clustered 95% bootstrap lower bound for `recall(cascade) - recall(transformer-only)` on NCSC `gold` positives must be >= `-0.02`; stage 2 must be invoked for <= 30% of the primary reference manifest; pooled p95 across all measured concurrency-64 requests must be <= 200 ms; and the stated request-error numerator divided by all 50,000 measured concurrency-64 requests must be < 0.1%. |

## Basis of Numerical Targets

All numerical values in the decision rules are study-defined operating gates, not prescribed by the literature and not already achieved by prior work. The `<= 1%` FPR ceiling, `<= 200 ms` p95 latency, `< 0.1%` request errors, `2000 ms` client timeout, `>= 80%` shift-window detection, `<= 5%` false alerts, `-0.02` recall-noninferiority margin, and `<= 30%` transformer invocation are prospective design choices frozen in protocol v1.3. Prior measurements motivate evaluation but do not establish compliance with these gates.

## Continuity With the Original Project

| Original commitment | Proposed confirmatory role | Continuity |
|---|---|---|
| Transformer-based detection | A compact character-level transformer is the escalation model and the transformer-only comparator. | The transformer remains central to the detection question, but its incremental value is tested at a controlled operating point rather than assumed. |
| GMM modeling | A GMM becomes the primary unsupervised monitor for external covariate/source shift, with MMD and PSI as comparators. | The original GMM work remains part of the study as a testable monitoring question. Downstream error comparison, rather than the shift score alone, tests whether its alerts are useful for routing. |
| 300M-to-30M distillation | Selective transformer execution is proposed as the efficiency study. No distillation result or contribution will be claimed. | This tests the original efficiency objective through recall, invocation rate, and real-HTTP performance rather than a model-size comparison. |

The revised study links three questions: which representation improves low-FPR detection, when shift monitoring should broaden escalation, and whether selective execution is practical inline. The transformer and GMM remain central, while the earlier exploratory results are preserved but do not decide the new hypotheses. Frontier AI inference is the deployment context, not a claim that the underlying evaluation methods are new. The title remains **Automated Phishing Detection for Frontier AI Inference**.

## Research Safeguards

- Evidence from the prior exploratory phase remains preserved but may not be used to accept or reject H1, H2, or H3.
- No URL is fetched. Only public data with a documented license may be used.
- The publisher-defined outcome-label contract and the applicable common and dataset-specific mechanical rules above are applied without individual assignment, override, or exception. Every quarantine records the applicable mechanical reason and count.
- If a required dataset, license, hash, label field, or split manifest is missing or inconsistent, the affected test stops and the hypothesis remains undecided. No imputed or synthetic dataset may replace it.
- Controlled URL perturbations are used only as declared monitoring probes; they do not replace labeled development or external evidence.
- A measured schema-invalid API request retains the frozen FastAPI validation response and counts in the H3 request-error numerator; it is not converted to a phishing label or removed from the 50,000-request denominator. Timeouts and connection failures also remain in that denominator.
- The protocol, model artifacts, thresholds, software environment, outcome-label contract, common and dataset-specific mechanical rules, full-test population rule, published-file ordering rule, and routing and outcome denominator rules are frozen before PhishVN v4 record access. At that external-evaluation freeze, schema metadata, provenance, and encoding are verified before the PhishVN-specific rules are applied; the deterministic source-to-local mapping and realized exclusion and denominator counts, dataset hashes, row identifiers, and split and window manifests are then frozen before model predictions or inferential results are examined.
- The [`phiusiil-development-v1`](https://github.com/KrtiT/automated-phishing-detection-public/releases/tag/phiusiil-development-v1) GitHub Release is the source-freeze record for this milestone. It keeps the exact licensed UCI source archive outside Git history and ties it to the archive and CSV SHA-256 checksums recorded in `data/sources.json`.
- Negative, null, or mixed results under the locked analysis are reported without test-informed retuning. The PhishVN v4 `frozen full-test stream` is not reused as validation data.
- The project title remains unchanged.
- PhiUSIIL preparation and its aggregate record are complete. The next technical milestone is frozen raw-URL feature extraction and `length-only` and `Logistic-L1` baseline implementation using only the train and validation partitions. PhishVN v4 remains sealed until the complete external-evaluation freeze described above.

## Active Decisions

1. RQ1-H1, RQ2-H2, and RQ3-H3 remain the active research questions, hypotheses, evidence strata, and decision rules.
2. The outcome-label contract and dataset-specific mapping and quarantine rules are mechanical. Whole-group conflicts and duplicate handling are recorded without personal adjudication.
3. PhishVN v4 replaces v1 for the later external evaluation because it incorporates the corrected v3 data and completed audit documentation. Tranco remains a secondary popularity control rather than verified benign evidence.
4. The numerical targets and H3 request-error construct are frozen prospectively and will not be revised in response to observed test results.
5. The current technical milestone is frozen raw-URL feature extraction and `length-only` and `Logistic-L1` baseline implementation using only the train and validation partitions. The PhishVN external evaluation begins only after the complete freeze described above.
